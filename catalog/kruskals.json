{
  "title": "Kruskal's MST",
  "shape": "graph_mst",
  "description": "Minimum spanning tree via Kruskal + Union-Find.\n",
  "interface": "kruskals(graph) -> graph | null\n",
  "id": "kruskals",
  "cpp": {
    "fn": "optional<vector<vector<Edge>>> kruskals(const vector<vector<Edge>>& g)",
    "structs": [
      "struct Edge { int to; int64_t weight; };"
    ],
    "hint": "// return MST adjacency or nullopt"
  }
}
