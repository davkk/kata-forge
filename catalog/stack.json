{
  "title": "Stack",
  "shape": "commands",
  "description": "LIFO stack. push/pop/peek at the top. All ops O(1).\n",
  "interface": "push(x: int) -> void\npop() -> int | null\npeek() -> int | null\nsize() -> int\n",
  "example": "push 5,7,9; pop \u2192 9",
  "id": "stack",
  "cpp": {
    "class": "Stack",
    "members": ["struct Node { int val; Node* next; }", "Node *head = nullptr"],
    "methods": [
      "void push(int x)",
      "optional<int> pop()",
      "optional<int> peek()",
      "int size()"
    ]
  }
}
