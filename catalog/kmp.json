{
  "title": "KMP Search",
  "shape": "str2_scalar",
  "description": "First index of pattern in text, or -1. Empty pattern \u2192 0.\n",
  "interface": "kmp_search(text, pattern) -> int\n",
  "id": "kmp",
  "cpp": {
    "fn": "int kmp(const string& a, const string& b)",
    "hint": "// return scalar from two strings"
  }
}
