{
  "title": "Permutations",
  "shape": "list_lists",
  "description": "All permutations of distinct ints (order-insensitive judge).\n",
  "interface": "permutations(nums) -> int[][]\n",
  "id": "permutations",
  "cpp": {
    "fn": "vector<vector<int64_t>> subsets(const vector<int64_t>& a)",
    "hint": "// return list of lists (order-insensitive)"
  }
}
