{
  "title": "BT BFS",
  "shape": "tree_find",
  "description": "Breadth-first search for a value in a binary tree.\n",
  "interface": "bfs(root, needle) -> bool\n",
  "id": "bt_bfs",
  "cpp": {
    "fn": "bool solve(Node* root, int64_t needle)",
    "structs": [
      "struct Node { int val; Node* left; Node* right; };"
    ],
    "hint": "// return true if needle in tree"
  }
}
