{
  "title": "Array List",
  "shape": "commands",
  "description": "dynamic array: O(1) random access, amortized O(1) append, O(n) middle insert/remove.",
  "interface": "prepend(x) append(x) insertAt(i, x)\nget(i) -> int|null remove(x) -> int|null removeAt(i) -> int|null size() -> int",
  "example": "append 5,7,9; get(2) -> 9",
  "id": "array_list",
  "cpp": {
    "class": "ArrayList",
    "ctor": "int cap",
    "members": [
      "int cap",
      "int len",
      "int* data"
    ],
    "methods": [
      "void append(int x)",
      "void prepend(int x)",
      "void insertAt(int i, int x)",
      "optional<int> removeAt(int i)",
      "optional<int> remove(int x)",
      "optional<int> get(int i)",
      "int size()"
    ]
  }
}
