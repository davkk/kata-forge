{
  "title": "Subsets (Power Set)",
  "shape": "list_lists",
  "description": "subsets(a) returns the power set of the distinct ints.",
  "interface": "subsets(a: int[]) -> int[][]",
  "example": "a = [1,2] -> [[],[1],[2],[1,2]]",
  "id": "subsets",
  "cpp": {
    "fn": "vector<vector<int>> subsets(const vector<int>& a)",
    "hint": "// return all subsets of a"
  }
}
