{
  "title": "Coin Change",
  "shape": "coin",
  "description": "Min coins to make amount. -1 if impossible.\n",
  "interface": "coin_change(amount: int, coins: int[]) -> int\n",
  "id": "coin_change",
  "cpp": {
    "fn": "int64_t solve(int64_t amount, const vector<int>& coins)",
    "hint": "// return min coins (or -1)"
  }
}
