{
  "title": "Kadane's Algorithm",
  "shape": "list_scalar",
  "description": "max_subarray(a) returns the maximum contiguous subarray sum.",
  "interface": "max_subarray(a: int[]) -> int",
  "example": "a = [-2,1,-3,4,-1,2,1,-5,4] -> 6",
  "id": "kadanes_algorithm",
  "cpp": {
    "fn": "int max_subarray(const vector<int>& a)",
    "hint": "// return max contiguous subarray sum"
  }
}
