{
  "title": "Queue",
  "shape": "commands",
  "description": "fifo queue: enqueue(x) at the tail, deque() from the head, peek() without removing; all O(1).",
  "interface": "enqueue(x) -> void deque() -> int|null peek() -> int|null size() -> int",
  "example": "enqueue 5,7,9; deque() -> 5; size() -> 2",
  "id": "queue",
  "cpp": {
    "class": "Queue",
    "members": [
      "struct Node { int val; Node* next; };",
      "Node* head",
      "Node* tail"
    ],
    "methods": [
      "void enqueue(int x)",
      "optional<int> deque()",
      "optional<int> peek()",
      "int size()"
    ],
    "ctor": ""
  }
}
