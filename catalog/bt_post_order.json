{
  "title": "BT Post-Order",
  "shape": "tree_list",
  "description": "Post-order traversal: left, right, root.\n",
  "interface": "post_order(root) -> int[]\n",
  "id": "bt_post_order",
  "cpp": {
    "fn": "vector<int> pre_order(Node* root)",
    "structs": [
      "struct Node { int val; Node* left; Node* right; };"
    ],
    "hint": "// return traversal order"
  }
}
