{
  "title": "BT In-Order",
  "shape": "tree_list",
  "description": "In-order traversal: left, root, right.\n",
  "interface": "in_order(root) -> int[]\n",
  "id": "bt_in_order",
  "cpp": {
    "fn": "vector<int> pre_order(Node* root)",
    "structs": [
      "struct Node { int val; Node* left; Node* right; };"
    ],
    "hint": "// return traversal order"
  }
}
