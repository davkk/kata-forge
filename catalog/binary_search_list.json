{
  "title": "Binary Search",
  "shape": "search",
  "description": "search(a, needle) returns true if needle is in sorted a.",
  "interface": "search(a: int[], needle: int) -> bool",
  "example": "a = [1,3,5,7,9], needle 5 -> true",
  "id": "binary_search_list",
  "cpp": {
    "fn": "bool search(const vector<int>& a, int needle)",
    "hint": "// return true if needle is in a"
  }
}
