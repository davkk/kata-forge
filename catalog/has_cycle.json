{
  "title": "Linked List Cycle Detection (Tortoise and Hare)",
  "shape": "cycle",
  "description": "has_cycle(head) returns true if the linked list contains a cycle.",
  "interface": "has_cycle(head: Node) -> bool",
  "example": "0->1->2->1 (cycle) -> true",
  "id": "has_cycle",
  "cpp": {
    "fn": "bool has_cycle(ListNode* head)",
    "structs": [
      "struct ListNode { int val; ListNode* next; };"
    ],
    "hint": "// return true if the list has a cycle"
  }
}
