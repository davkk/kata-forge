{
  "title": "LRU Cache",
  "shape": "commands",
  "description": "Least-recently-used cache, capacity via init. get/update by string key, int value.\n",
  "interface": "update(k,v) get(k)\n",
  "id": "lru",
  "cpp": {
    "class": "LRU",
    "methods": [
      "void update(const string& k, int64_t v)",
      "optional<int64_t> get(const string& k)",
      "int size()"
    ]
  }
}
