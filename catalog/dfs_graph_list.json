{
  "title": "DFS on a Graph (Adjacency List)",
  "shape": "graph_path",
  "description": "dfs(g, source, sink) returns a node path from source to sink, or nullopt.",
  "interface": "dfs(g: graph, source: int, sink: int) -> int[] | null",
  "example": "adjacency list with a 0->1->2 path",
  "id": "dfs_graph_list",
  "cpp": {
    "fn": "optional<vector<int>> dfs(const vector<vector<Edge>>& g, int source, int sink)",
    "structs": [
      "struct Edge { int to; int weight; };"
    ],
    "hint": "// return node path from source to sink, or nullopt"
  }
}
