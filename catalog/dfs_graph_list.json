{
  "title": "DFS Graph (List)",
  "shape": "graph_path",
  "description": "DFS path on weighted adj list from source to sink, or null.\n",
  "interface": "dfs(graph, source, sink) -> int[] | null\n",
  "id": "dfs_graph_list",
  "cpp": {
    "fn": "optional<vector<int>> dfs(const vector<vector<Edge>>& g, int source, int sink)",
    "structs": [
      "struct Edge { int to; int64_t weight; };"
    ],
    "hint": "// return path or nullopt"
  }
}
