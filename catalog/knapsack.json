{
  "title": "0/1 Knapsack",
  "shape": "knapsack",
  "description": "Max value with weight capacity; each item once.\n",
  "interface": "knapsack(capacity, weights, values) -> int\n",
  "id": "knapsack",
  "cpp": {
    "fn": "int64_t knapsack(int64_t capacity, const vector<int>& weights, const vector<int>& values)",
    "hint": "// return max value"
  }
}
