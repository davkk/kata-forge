{
  "title": "Trie",
  "shape": "commands",
  "description": "Prefix tree: insert/delete words, find all words with a prefix.\n",
  "interface": "insert(s) delete(s) find(prefix) -> string[]\n",
  "id": "trie",
  "cpp": {
    "class": "Trie",
    "members": ["struct Node { Node* child[26]; bool isWord = false; }", "Node* root"],
    "methods": [
      "void insert(const string& word)",
      "void remove(const string& word)",
      "vector<string> find(const string& prefix)"
    ]
  }
}
