{
  "title": "Ring Buffer",
  "shape": "commands",
  "description": "Fixed-capacity circular buffer. push, pop, get(idx from head).\n",
  "interface": "push(x) pop() get(i) length()\n",
  "id": "ring_buffer",
  "cpp": {
    "class": "RingBuffer",
    "members": ["int cap"],
    "methods": [
      "void push(int x)",
      "optional<int> pop()",
      "optional<int> get(int i)",
      "int size()"
    ]
  }
}
