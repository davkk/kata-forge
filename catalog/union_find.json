{
  "title": "Union-Find (Disjoint Set Union)",
  "shape": "commands",
  "description": "disjoint set over n elements 0..n-1: find(x), unite(x, y), connected(x, y), size().",
  "interface": "find(x) -> int unite(x, y) -> bool connected(x, y) -> bool size() -> int",
  "example": "n=5: unite(0,1), connected(0,1) -> true",
  "id": "union_find",
  "cpp": {
    "class": "UnionFind",
    "ctor": "int n",
    "methods": [
      "int find(int x)",
      "bool unite(int x, int y)",
      "bool connected(int x, int y)",
      "int size()"
    ]
  }
}
