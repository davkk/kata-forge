{
  "title": "Dijkstra (List)",
  "shape": "graph_path",
  "description": "Shortest weighted path source\u2192sink on adj list. Return node path.\n",
  "interface": "dijkstra(graph, source, sink) -> int[]\n",
  "id": "dijkstra_list",
  "cpp": {
    "fn": "optional<vector<int>> dijkstra(const vector<vector<Edge>>& g, int source, int sink)",
    "structs": [
      "struct Edge { int to; int64_t weight; };"
    ],
    "hint": "// return path or nullopt"
  }
}
