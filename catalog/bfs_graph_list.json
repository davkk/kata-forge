{
  "title": "BFS Graph (List)",
  "shape": "graph_path",
  "description": "BFS shortest path (by hops) on weighted adj list. Return node path or null.\n",
  "interface": "bfs(graph, source, sink) -> int[] | null\n",
  "id": "bfs_graph_list",
  "cpp": {
    "fn": "optional<vector<int>> bfs(const vector<vector<Edge>>& g, int source, int sink)",
    "structs": [
      "struct Edge { int to; int weight; };"
    ],
    "hint": "// return path or nullopt"
  }
}
