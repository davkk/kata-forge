{
  "title": "BFS Graph (Matrix)",
  "shape": "matrix_path",
  "description": "BFS path on adjacency matrix (0 = no edge). Path or null.\n",
  "interface": "bfs(matrix, source, sink) -> int[] | null\n",
  "id": "bfs_graph_matrix",
  "cpp": {
    "fn": "optional<vector<int>> solve(const vector<vector<int64_t>>& g, int source, int sink)",
    "hint": "// return path or nullopt"
  }
}
